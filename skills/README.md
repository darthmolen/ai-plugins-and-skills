# Skills

This folder holds the per-host installation guides for using the CSAT skill catalog. If you're looking for the skill catalog itself (browse by category, click into individual skills), that's at [the repo root README](../../README.md#skills). If you want to understand what a "skill" *is* and how to install them in your AI tool of choice, you're in the right place.

---

## What is a skill?

A **skill** is a piece of situational knowledge that you give to an AI agent. It's not a model, not a plugin, not a chat prompt — it's a discrete chunk of expertise the agent loads only when the situation calls for it. Skills usually take one of three shapes:

- **Repeatable processes** — "here's how we run a code review on this team," "here's our RED-GREEN-REFACTOR loop," "here's how we draft an ADR."
- **Tools the agent can use** — a script the agent invokes when needed (`sql-query`, `bulk-rename`, `azure-servicebus-operations`), with the SKILL.md explaining when to reach for it and how to read its output.
- **General knowledge about a subject** — coding standards for a language, architectural patterns for a stack, the operational quirks of a specific service.

The point is collaboration. You and the agent are working together; skills are the shared vocabulary that keeps both of you on the same page without you having to re-explain everything in every prompt. The agent loads the skill when its description matches what you're doing — no manual ceremony required in most harnesses.

Skills follow the open [**Agent Skills specification**](https://agentskills.io) — a single `SKILL.md` per skill, with YAML frontmatter naming the skill and describing when to activate it, followed by Markdown instructions for the agent. Because the format is portable, the skills in this repo work across Claude Code, Copilot CLI (terminal and VS Code), VS Code Copilot, and Visual Studio 2026 — same files, different host wiring.

---

## Per-host installation guides

| Host | Guide | Notes |
|---|---|---|
| Claude Code | [README-CLAUDECODE.md](README-CLAUDECODE.md) | Fully supported — marketplace install with slash commands |
| Copilot CLI (terminal) | [README-COPILOT-CLI.md](README-COPILOT-CLI.md) | Direct skill symlinks recommended; plugin install as fallback |
| VS Code + Copilot CLI Chat extension | [README-COPILOT-CLI-VSCODE-EXTENSION.md](README-COPILOT-CLI-VSCODE-EXTENSION.md) | Claude-Code-lite sidebar around Copilot CLI |
| VS Code Copilot (native) | [README-VSCODE-COPILOT.md](README-VSCODE-COPILOT.md) | Agent mode via `chat.agentSkillsLocations` |

The skills themselves are identical across all five — what changes is how each host discovers and loads them. Pick the guide that matches the tool you actually live in.

---

## Where the skills live

- **Source of truth:** [`/skills/<name>/`](../../skills/) at the repo root. Each skill is self-contained — `SKILL.md` plus any `scripts/` or `references/` it needs.
- **Plugin bundles:** `claude-code/plugins/<plugin>/skills/<name>/` — generated by `scripts/build-plugin.ps1` from `/skills/<name>/` for harnesses that install via plugin marketplace. These are gitignored; rerun the build after a `git pull` if you install through a plugin bundle.

For the workflows that *use* these skills (spec-driven coding, plan review, code review), see [documentation/workflows/](../workflows/README.md).
